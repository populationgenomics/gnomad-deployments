apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
spec:
  template:
    spec:
      containers:
      - name: redis-server
        resources:
            requests:
              cpu: '500m'
              memory: '4Gi'
            limits:
              cpu: 1
              memory: '4Gi'
